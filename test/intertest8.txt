/*
 * Testing modification to reduce I/O operations by storing characters when
 * transitioning between two tokens, rather than reading the same character from
 * the file twice.
 * This required a slight modification to parseError(), and the following are
 * edge cases to test that modification.
 */

10 variables
x1 + x2 <= 3
x1 - x3 <=
x1 - x4 <=
x2 + x5